<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Sync PostgreSQL auto-increment sequences after explicit-id seed inserts.
      ChangeSet 012 inserts the admin user with id=1 directly; without this the
      underlying sequence still starts at 1 and the very first real INSERT
      collides with the seeded row ("duplicate key value violates unique
      constraint users_pkey").
    -->
    <changeSet id="015-sync-sequences" author="capstone" dbms="postgresql">
        <sql>
            SELECT setval(pg_get_serial_sequence('users', 'id'), COALESCE((SELECT MAX(id) FROM users), 1));
            SELECT setval(pg_get_serial_sequence('seller_profiles', 'id'), COALESCE((SELECT MAX(id) FROM seller_profiles), 1));
            SELECT setval(pg_get_serial_sequence('categories', 'id'), COALESCE((SELECT MAX(id) FROM categories), 1));
            SELECT setval(pg_get_serial_sequence('products', 'id'), COALESCE((SELECT MAX(id) FROM products), 1));
            SELECT setval(pg_get_serial_sequence('addresses', 'id'), COALESCE((SELECT MAX(id) FROM addresses), 1));
            SELECT setval(pg_get_serial_sequence('orders', 'id'), COALESCE((SELECT MAX(id) FROM orders), 1));
            SELECT setval(pg_get_serial_sequence('order_items', 'id'), COALESCE((SELECT MAX(id) FROM order_items), 1));
            SELECT setval(pg_get_serial_sequence('payments', 'id'), COALESCE((SELECT MAX(id) FROM payments), 1));
            SELECT setval(pg_get_serial_sequence('ratings', 'id'), COALESCE((SELECT MAX(id) FROM ratings), 1));
            SELECT setval(pg_get_serial_sequence('gift_points', 'id'), COALESCE((SELECT MAX(id) FROM gift_points), 1));
            SELECT setval(pg_get_serial_sequence('audit_log', 'id'), COALESCE((SELECT MAX(id) FROM audit_log), 1));
        </sql>
    </changeSet>
</databaseChangeLog>